@model IEnumerable<WebApplicationGrupp13.Models.EducationalPostCategory>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.category }) |
            @Html.ActionLink("Details", "Details", new { id=item.category }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.category })
        </td>
    </tr>
}

</table>
