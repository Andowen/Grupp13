@model IEnumerable<WebApplicationGrupp13.Models.FormalBlogPost>

@{
    ViewBag.Title = "Index";
}
@if (Request.IsAuthenticated) {
    <h2>Index</h2>

    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    @*<table class="table">*@
    @*<tr>
            <th>
                @Html.DisplayNameFor(model => model.postText)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.dateTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.creator)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.category)
            </th>
            <th></th>
        </tr>*@

    foreach (var item in Model) {
        @*<tr>
                <td>
                    @Html.DisplayFor(modelItem => item.title)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.postText)
                </td>

                <td>
                    @Html.DisplayFor(modelItem => item.dateTime)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.creator)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.category)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.id })
                </td>
            </tr>*@
<div class="formalBlogPost">
    <h3>@Html.DisplayFor(modelItem => item.title)<span class="authorTime">@Html.DisplayFor(modelItem => item.creator)@Html.DisplayFor(modelItem => item.dateTime)</span></h3>
        <p style="float: right"></p>
        <p style="float: right"></p>
        <p>@Html.DisplayFor(modelItem => item.postText)</p>
        @if (item.creator.Equals(User.Identity.Name)) {
            @Html.ActionLink("Edit", "Edit", new { id = item.id });
@Html.ActionLink("Details", "Details", new { id = item.id });
@Html.ActionLink("Delete", "Delete", new { id = item.id });

}
    </div>

}

@*</table>*@
} else {
<h1>Du måste logga in för att komma åt denna sida!</h1>
}
